<!doctype html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        {% if title %}
        <title>{{ global.me }} - {{ title }}</title>
        {% else %}
        <title>{{ global.me }}</title>
        {% endif %}
        <link href="/asset/style.css" rel="stylesheet">
    </head>
    <body>
        {{ content }}
    </body>
</html>
